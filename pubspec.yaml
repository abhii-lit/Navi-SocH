name: navi_soch
description: "Navi SocH - A rural learning app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  shared_preferences: ^2.5.3
  video_player: ^2.9.1
  confetti: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

  assets:
    - assets/data.json
    - assets/images/

# 🔹 Config for App Icon
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.jpg" # ← Put your logo here
  min_sdk_android: 21
